# Watch 專案協作指引

本檔保留跨功能原則與文件索引。依任務查閱相關章節，不需在每次修改前讀完整份專案文件。

## 專案與核心規則

- Next.js／React 網站搭配 Electron 桌面版；本機開發方式與環境變數見 [README](README.md#本機開發)，可用指令以 `package.json` 為準。
- 正式網站在 Railway；Vercel 部署保留給 Cron。不要刪除 `vercel.json`，也不要把 Railway 正式網站服務改成 Cron 服務。
- 介面以繁體中文為主，保留既有 UI 文案與使用流程，除非本次需求明確要調整。
- 搜尋、首頁推薦與 TMDB 公開內容允許匿名查看；桌面版與網站共用帳號及資料，不支援離線寫入使用者資料。
- 資料正確性優先於短暫可用性：避免刪帳復活、跨帳號讀寫，以及 identity mapping 失敗時 fallback 成另一個 user id。
- Watch 業務資料與共用 Auth 分屬 `DATABASE_URL`、`AUTH_DATABASE_URL` 的責任範圍。刪除 Watch 資料與刪除共用帳號是不同操作；後者也不代表刪除其他網站的業務資料。
- 私人資料與快取按帳號隔離。讀取失敗須保留適用的上次成功資料並顯示錯誤，不得把失敗當成空清單／零筆或用來回寫 state。
- 觀看日期使用 date-only 語意、寫入 UTC 午夜；播出／上映判斷使用台北日期。使用者觀看進度與 TMDB 公開資料分開管理。
- 即時連線正常時不另加常駐輪詢；fallback 僅用於斷線。TMDB 快取保留固定期限、冷卻與請求共用，避免為顯示或同步新增高頻查詢。

## 按任務查閱

| 涉及的變更 | 對應文件 |
| --- | --- |
| auth、帳號刪除、觀看紀錄、好友分享、交易鎖、TV state 寫入、私人讀取與 migration | [帳號與資料一致性](docs/engineering/accounts-and-data.md) |
| 清單排序、提醒、卡片、搜尋、DetailModal 互動 | [清單與詳情](docs/engineering/watchlist-ui.md) |
| 月曆版面、日期範圍、跨日卡片與參與者可見性 | [月曆](docs/engineering/calendar.md) |
| SSE、Redis Pub/Sub、fallback、活動狀態、revision 與通知 | [即時同步](docs/engineering/realtime.md) |
| TMDB 端點、集數分母、資料新鮮度、標題、快取與限流 | [TMDB 與集數資料](docs/engineering/tmdb.md) |
| Electron、身份快取、protocol 串流與更新閘門 | [桌面版](docs/engineering/desktop.md) |
| 部署、Cron、共用 Redis、代理設定或正式發布 | [部署與發布](docs/engineering/deployment.md) |

跨功能變更再補讀相關文件。純文案或排版修改只需查該畫面的規則；涉及 TMDB API 或外部規範時，依 TMDB 文件內的索引查對應官方資料。

## 執行範圍與完成條件

- 使用者已授權的實作／修正，持續完成必要修改、相關驗證及本次變更造成的失敗修復；最後交代結果與仍有的限制。使用者只要求審查或分析時，以完成評估為範圍。
- 會影響核心資料、部署或資料一致性時，先簡述影響與驗證方式。符合既有產品規則的本機修正可在說明後繼續；「先說明」不等於等待批准。
- 已接受的產品取捨依主題文件執行。只有需求尚未決定、會改變既有產品定義，或會明顯降低 auth／session／限流／realtime 可用性的新方案，才提出具體差異請使用者決定；等待時繼續不依賴該決策的工作。
- 同一取捨若讓 review 反覆要求相反修正，指出衝突並對齊一次，再依決定完成；不要反覆改回相同邏輯。
- 修改 schema／產生 migration 檔、提交程式碼與套用資料庫分開處理。只有已明確授權的目標環境才可套用 migration／`db:push`；正式部署、發布或正式資料操作也須在既有授權範圍內。授權已明確時不重複詢問。
- 若最後一步仍需授權，先完成可供審查的差異、影響說明與可行的本機驗證，再指出具體待批准操作。不要因該步尚未授權而停止其餘已授權工作。

## 驗證與提交

依變更選擇足以驗證行為的檢查；通過後，只在有新修改、失敗或未解疑慮時擴大或重跑。

| 變更類型 | 驗證方式 |
| --- | --- |
| 純文件／註解 | 檢查引用、規則一致性及 `git diff --check`；不為此新增行為測試或跑完整 build |
| UI 文案 | `npm run check:locale`；檢查繁中、亂碼與可疑問號字串 |
| 功能、API 或資料流程 | `npm test -- <受影響測試檔>`，並按影響手動驗證登入、清單、觀看紀錄或好友流程；跨流程修改才擴大範圍 |
| 編譯、依賴、啟動或部署設定 | 相關測試與 `npm run build`；不得把「本機執行」等同於資料來源已隔離 |
| Electron 串流／身份快取 | [桌面版](docs/engineering/desktop.md#本機驗證) 中的對應隔離腳本 |
| 準備發布 | [發布檢查](docs/engineering/deployment.md#發布)，包含依賴警報與安裝檔一致性 |

- 程式碼提交前執行 `npm run lint` 與 `npm run check:locale`，完成受影響的功能驗證；純文件／註解提交依上表即可。新增 CI 時可沿用語系檢查指令。
- 已確認使用 mocks／本機可丟棄 fixtures 的測試，可執行、修復本次修改造成的失敗並重跑，不必逐步詢問。未確認資料來源的腳本不能直接視為隔離測試。
- 檢查失敗時依輸出定位原因、修復並重跑受影響檢查；既有或無關失敗另行說明，不為消除錯誤而擴大本次產品範圍。
- 提交前用 `git status -sb` 確認變更，保留使用者既有工作。commit 訊息使用中文，一筆聚焦一件事；不相關重構與功能修改分開。

## 文件維護

- 產品規則、重要取捨或資料契約變更時，同步更新對應主題文件；只有跨功能原則、協作流程或文件路由改變才更新本檔。
- 將修正规則併回所屬章節，合併重複說明、移除已被取代的敘述，不在主檔持續追加日期式修正日誌。
- 保留不直觀的產品限制、原因與已知平台限制；實作細節優先留在對應主題文件、程式註解或有意義的測試中。新增主題文件時，補上本檔的適用條件與連結。
- 文件與程式不一致時，先核對相關程式、測試及本次需求；能確認是文件過期就修正文案，涉及尚未決定的產品行為則指出差異，不能直接把程式現況當成新規則。
